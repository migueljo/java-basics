package learningjavabook.chapter6;

public class Assertions {
	/**
	 *  assert false;
	 * 	assert array.length > min;
	 * 	assert a > 0 : a // shows value of "a" to the user
	 * 	assert foo != null : "foo is null" // shows message "foo is null" to the user
	 */
}
